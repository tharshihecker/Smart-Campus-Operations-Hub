spring:
  application:
    name: smartcampus-backend

  data:
    mongodb:
      uri: ######################################
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 20MB

  mail:
    host: smtp.gmail.com
    port: 587
    username:#########################
    password: ################
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true   # kept from feature branch (good practice)

server:
  port: 8080

app:
  jwt:
    secret: SmartCampusSLIIT2026VerySecretKeyForJWTAuthenticationMinimum256Bits
    expiration-ms: 86400000

  google:
    client-id: ###########################

  mail:
    enabled: true          # from main (feature-safe toggle)
    from: v###################   # merged from both

cloudinary:
  cloud-name:##########
  api-key: #################
  api-secret: ##########################
